# Optional Docker Compose for library development
# Usage: docker-compose run --rm library-dev

services:
  library-dev:
    container_name: library-dev
    build: .
    volumes:
      - .:/workspace
    command: uv run pytest
